#!/bin/sh

#Create empty directory as name of component
mkdir -p src/components/$1

#Add an empty scss file to this component directory
touch src/components/$1/$1.scss



#Copy template code to create default template of this component
sed -e "s/COMPONENT_NAME/$1/g" lib/templates/defaultClassComponent.js > src/components/$1/$1.js